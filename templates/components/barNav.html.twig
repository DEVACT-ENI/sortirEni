<nav class="navbar bg-dark">
    <div class="container-fluid">
{#        <a class="navbar-brand text-white" href="{{ path('main_home') }}"><img class="logo"#}
{#                                                                               src="{{ asset('images/getbukkitwhite.svg') }}"#}
{#                                                                               alt="image">Bucket-List</a>#}
        <ul class="nav">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('main_home') }}">Home</a>
            </li>

            {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('participant_modif_profil') }}">Profile</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>